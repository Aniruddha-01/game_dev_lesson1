for i in range(1,6):
    grp_name = input('Enter the name of your group ')
    grp_size = int(input('Enter the size of your group '))
    comp_date = input('Enter the competition date ')
    venue = input('Enter the venue of the competition ')
    medal_type = input('Enter the type of medal recieved ')

    group_info = (grp_name,grp_size,comp_date,venue,medal_type)

    print("group number",i, group_info)


    
